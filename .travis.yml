os: osx
osx_image: xcode7.3
language: node_js
node_js:
  - "7"
  - "6"
  - "4"
install:
  - node --version
  - npm --version
  - npm install -g npm
  - npm install -g gulp
  - npm install
script:
  - _FORCE_LOGS=1 gulp once
after_success:
  - gulp coveralls
